# Django Movie

## Run with Docker

Run:
```
sh build.sh
```
Then:
```
docker compose up
```

## Testing

```
make test
```

This will run tests in both users and movies folders.

### How to run with the `coverage` package?

```
make test-coverage
```

Then run:

```
make coverage
```
